---
status: pending
wave: 3
depends_on: [3]
skill: do-task
reviewers: code-reviewer
verify_smoke: ""
verify_user: ""
teammate_name: ""
files_to_modify: svedeniya-ob-obrazovatelnoy-organizatsii.html
files_to_read: index.html (lines 1100+ for JavaScript)
---

# Task 3.3: Скопировать JavaScript и глобальные элементы

## Description
Скопировать глобальные UI элементы (progress bar, back-to-top, geometry) и JavaScript код для интерактивности.

## Acceptance Criteria
1. Глобальные элементы до `<main>`:
   - Progress bar прокрутки (`#scroll-progress`)
   - Кнопка «Наверх» (`#back-to-top`)
   - Vertical brand (hidden lg:block)
   - Геометрический фон (`.global-geo`)
2. JavaScript код:
   - Scroll progress tracking
   - Back-to-top button toggle
   - Smooth scroll
   - Mobile menu toggle
3. Код обернут в DOMContentLoaded
4. Проверка существования элементов перед манипуляцией

## Technical Details
**Copy from index.html:**
- Lines ~50-80: global UI elements
- Lines ~1100-1200: JavaScript code

**Safety pattern:**
```javascript
document.addEventListener('DOMContentLoaded', function() {
    // Check element exists before manipulating
    const scrollProgress = document.getElementById('scroll-progress');
    if (scrollProgress) {
        // ...
    }

    const backToTop = document.getElementById('back-to-top');
    if (backToTop) {
        // ...
    }
});
```

## Definition of Done
- Глобальные элементы скопированы
- JavaScript добавлен
- Нет ошибок в консоли
- Анимации работают
